<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kosta.springjspblog.repository.mybatis.CategoryMapper">

    <insert id="save" parameterType="Category" useGeneratedKeys="true" keyProperty="categoryNo">

        insert into kostablog.category(categoryNo, id, categoryName, categoryDescription, regDate)
        values(
        #{categoryNo},
        #{id},
        #{categoryName},
        #{categoryDescription},
        sysdate()
        )

        <selectKey resultType="String" keyProperty="id" keyColumn="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>

    </insert>


    <select id="findById" parameterType="string" resultType="Blog">
        SELECT categoryNo, id, categoryName, categoryDescription, regDate
        FROM kostablog.category
        WHERE id = #{id}
    </select>




</mapper>

