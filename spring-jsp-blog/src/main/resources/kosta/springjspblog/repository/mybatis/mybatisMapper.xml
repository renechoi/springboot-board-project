<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kosta.springjspblog.repository.mybatis.MybatisMapper">

    <insert id="save" parameterType="User" useGeneratedKeys="true" keyProperty="userNo">

        <selectKey keyProperty="userNo" resultType="int" order="BEFORE">
            SELECT MAX(userNo) + 1 FROM kostablog.users
        </selectKey>

        insert into kostablog.users(userNo, id, userName, password, joinDate)
        values(
        #{userNo},
        #{id},
        #{userName},
        #{password},
        sysdate())

        <!--        <selectKey resultType="int" keyProperty="userNo" keyColumn="userNo" order="AFTER">-->
        <!--            SELECT LAST_INSERT_ID()-->
        <!--        </selectKey>-->
    </insert>


    <select id="findById" parameterType="string" resultType="User">
        SELECT userNo, id, userName
        FROM kostablog.users
        WHERE userNo = #{id}
    </select>

    <!-- 로그인용(id,password 로 회원정보 가져오기 -->
    <select id="findByObject" parameterType="User" resultType="User">
        SELECT userNo, id, userName
        FROM kostablog.users
        WHERE id = #{id}
          AND password = #{password}
    </select>


</mapper>


        <!--        &lt;!&ndash; 아이디로 회원정보 가져오기 &ndash;&gt;-->
        <!--<select id="selectUserById" parameterType="string"  resultType="com.javaex.vo.UserVo">-->
        <!--<![CDATA[-->
        <!--select userNo, id, userName from users-->
        <!--where id = #{id}-->
        <!--]]>-->
        <!--	</select>-->


        <!--        &lt;!&ndash; id 로 usrNo 가져오기 &ndash;&gt;-->
        <!--<select id="selectUserNoById" parameterType="string"  resultType="int">-->
        <!--<![CDATA[-->
        <!--select userNo-->
        <!--from users-->
        <!--where id = #{id}-->
        <!--]]>-->
        <!--	</select>-->